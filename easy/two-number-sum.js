// input: array of distinct integers, integer which is target sum
// output: if any numbers in array add up to target sum, return array of those two numbers. otherwise return an empty array.

// note: can assume that there will be at most one pair of numbers summing up to the target sum. additionally, you cannot add a single integer to itself to reach the sum.

function twoNumberSum(array, targetSum) {
    
}

twoNumberSum()
